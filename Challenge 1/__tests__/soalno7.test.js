const getTotalPenjualan = require('../Soal-7/script');

describe('SUM of Total Terjual From an Array of Objects', () => {
  test('should ', () => {
    expect(
      getTotalPenjualan()
    ).toBe(307);
  });
});
